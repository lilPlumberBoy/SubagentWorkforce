# Orchestrator TODO

## Next Runtime / Dashboard Improvements

- DONE: Move worker assignment and worker-role selection completely out of planning prompts. Planning now defines the work only; orchestration assigns concrete worker prompts after the plan is accepted.
- DONE: Parallelize `plan-phase` across objectives so later objectives do not remain at `0%` while the first objective is still planning.
- DONE: Stream planner stdout/stderr artifacts to disk while planning processes are still running instead of writing them only at process exit. Planning now appends stdout/stderr live, preserves partial stream files on interruption, and rotates internal retry attempts onto new stream artifacts so earlier attempt evidence is not overwritten.
- DONE: Shrink capability-manager prompts by passing compact summaries where possible instead of repeating the full goal and outline payloads. Capability planning prompts now use tighter semantic sections, skip empty context entries, and record materially smaller prompt sizes while preserving the exact lane contract and input-addressability rules.
- DONE: Stop reusing planner-generated lane `objective` text inside the human-facing `This objective is trying to achieve` section of capability planning prompts. Capability prompts now keep objective meaning in the objective section, explain the objective id as a lookup handle, and move lane-specific planning intent under `What This Capability Should Plan`.
- DONE: Reconcile stale live activities and recover cleanly when a planning or execution connection is interrupted so runs do not remain stuck in phantom `finalizing` states. Planning and execution activities now reconcile to `interrupted` or recovered terminal states, run-state is refreshed from reconciled activity truth, and stale autonomy `active` status is downgraded when no live work remains.
- DONE: Stop relaunching the same blocked `polish` validation after the repair budget is exhausted. Once `polish` is already on `hold`, the controller now freezes the blocked task/report state instead of requeueing the same validation, and reconciliation clears stale autonomy `active` status when no live work remains.
- DONE: Add a flag to not require human approval at all for a run. `--no-review-gates` now drives fully autonomous advancement through `advance` review gates without requiring human approval records.
- DONE: Fix autonomy approval-scope semantics. `--approval-scope none` now means "no review gate / continue automatically" for advanceable phases, while `hold` recommendations still pause for intervention.
- DONE: Make autonomous runs use the same dashboard progress model and lifecycle visibility as manual runs. Autonomy state is now controller-only (`running`, `waiting_for_approval`, `stopped`, `completed`), it stays `running` while live work is active, and it pauses as `waiting_for_approval` at review gates instead of collapsing into an early `stopped` state.
- DONE: Add monitoring for different statistics around the calls to the llm during a run, these should include things like response time, amount of tokens used, size of the prompt, and any other data that would be relevant for an engineer looking into the health of the system. Live activities and dashboards now surface prompt volume, queue wait, time to first stream, processing/runtime/wall-clock timing, token usage, retries, and stream-byte counters for both planning and execution calls.
- DONE: Add a dedicated debug-prompt view for live processes that shows each prompt exactly as submitted, plus prompt metadata like submit time, queue wait, processing duration, token usage, and the currently running attempt/workspace. The new `debug-prompt` command renders per-activity prompt diagnostics with prompt metadata, timing breakdowns, token counts, workspace/log paths, latest events, and an optional full prompt body view.
- DONE: Recovery should ignore blocked, rejected, and otherwise stale bundles when checking for missing landing results, and bundle repair/replan should archive obsolete bundle incidents so `resume-phase` only reasons about active accepted landing work. Recovery and phase reports now filter bundle state through the current manager-approved bundle set, and obsolete bundle recovery summaries are archived out of active incident tracking.
- DONE: Fix the final autonomy phase-advance event when a run completes. Terminal completion now emits a dedicated `autonomy.completed` record instead of the bogus self-transition `Advanced from polish to polish.`.

- TODO: Prevent reconciliation/autonomy state from regressing to `stopped` while active or queued work still exists later in the same run. `auto-todo-mvp-089-full` reached `polish` with active implementation/validation work, but `autonomy.json` was rewritten to `status: stopped` with the stale reason `Phase work was interrupted or reconciled, and no live work remains.` even though [run-state.json](/Users/mike/projects/personal/SubagentWorkforce/runs/auto-todo-mvp-089-full/live/run-state.json) still showed active and queued activities.
- TODO: Make planning-repair prompts regenerate their lane contract and response schema wrapper from the normalized post-validation context instead of replaying stale invalid contract text. In `auto-todo-mvp-096-full`, the backend repair prompt correctly said the workspace was JavaScript, but it still embedded `.ts` required final outputs in the `Lane Contract` and still asked for `capability-plan.response.v1` even though the accepted repaired output was a `capability-plan.v1` plan with `.js` paths.

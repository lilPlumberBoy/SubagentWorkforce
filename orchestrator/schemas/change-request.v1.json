{
  "type": "object",
  "required": ["schema", "run_id", "change_id", "summary", "impact", "requested_by"],
  "additionalProperties": false,
  "properties": {
    "schema": {"type": "string", "enum": ["change-request.v1"]},
    "run_id": {"type": "string"},
    "change_id": {"type": "string"},
    "summary": {"type": "string"},
    "requested_by": {"type": "string"},
    "impact": {
      "type": "object",
      "required": [
        "goal_changed",
        "scope_changed",
        "boundary_changed",
        "interface_changed",
        "architecture_changed",
        "team_changed",
        "implementation_changed"
      ],
      "additionalProperties": false,
      "properties": {
        "goal_changed": {"type": "boolean"},
        "scope_changed": {"type": "boolean"},
        "boundary_changed": {"type": "boolean"},
        "interface_changed": {"type": "boolean"},
        "architecture_changed": {"type": "boolean"},
        "team_changed": {"type": "boolean"},
        "implementation_changed": {"type": "boolean"}
      }
    }
  }
}
